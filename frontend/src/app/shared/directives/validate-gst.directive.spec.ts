import { ValidateGSTDirective } from './validate-gst.directive';

describe('ValidateGSTDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateGSTDirective();
    expect(directive).toBeTruthy();
  });
});
