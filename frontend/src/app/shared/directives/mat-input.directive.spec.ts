import { MatInputDirective } from './mat-input.directive';

describe('MatInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MatInputDirective();
    expect(directive).toBeTruthy();
  });
});
