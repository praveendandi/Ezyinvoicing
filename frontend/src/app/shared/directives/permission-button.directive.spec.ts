import { PermissionButtonDirective } from './permission-button.directive';

describe('PermissionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PermissionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
