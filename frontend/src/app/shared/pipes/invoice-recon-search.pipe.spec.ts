import { InvoiceReconSearchPipe } from './invoice-recon-search.pipe';

describe('InvoiceReconSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceReconSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
