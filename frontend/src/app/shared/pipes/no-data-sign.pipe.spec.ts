import { NoDataSignPipe } from './no-data-sign.pipe';

describe('NoDataSignPipe', () => {
  it('create an instance', () => {
    const pipe = new NoDataSignPipe();
    expect(pipe).toBeTruthy();
  });
});
